# Implementation: Update Media

This document outlines the media updates made to the horizontal scrolling section.

## Completed Tasks

- [x] Locate the component handling horizontal scrolling videos (`src/components/stride/scenes/HorizontalRail.tsx`).
- [x] Trace the media source to the `stridemedia` object in `src/lib/stride-media.ts`.
- [x] Update the 1st video (Pull-up bar, `7671776`) in the `rail` array and configure it to start at the 3-second mark (`#t=3`).
- [x] Update the 2nd video (Woman looking at camera, `7675448`) in the `rail` array.
- [x] Update the 3rd video (Black man walking at swimming pool, `9629859`) in the `rail` array.
- [x] Update the 4th video (Man getting surfboard at beach, `8678058`) in the `rail` array.
- [x] Update the 5th video (Men playing streetball, `5275681`) in the `rail` array.
- [x] Add a 6th video (Group of men hiking, `5624211`) to the `rail` array to ensure each of the 6 words in the scrolling section gets a unique background video.
